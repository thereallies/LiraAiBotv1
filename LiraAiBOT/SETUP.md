# 🚀 LiraAI MultiAssistent - Быстрый старт

## 📋 Что нужно для запуска

### 1. Python окружение

**Требуется:** Python 3.8 или выше

```bash
# Проверка версии Python
python --version
```

### 2. Установка зависимостей

```bash
# Перейдите в директорию проекта
cd /path/to/liraai-multiassistent

# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
# Для macOS/Linux:
source venv/bin/activate
# Для Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

```bash
# Скопируйте пример файла конфигурации
cp .env.example .env
```

Откройте `.env` и заполните необходимые ключи:

```env
# === ОСНОВНЫЕ API КЛЮЧИ ===
# OpenRouter API ключ (получите на https://openrouter.ai)
OPENROUTER_API_KEY=sk-or-v1-ваш_ключ

# === TELEGRAM ===
# Токен бота (получите от @BotFather в Telegram)
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# === ГЕНЕРАЦИЯ ИЗОБРАЖЕНИЙ (опционально) ===
# Hugging Face API ключ (если нужен)
HF_API_KEY=ваш_huggingface_key

# === ГОЛОСОВЫЕ ФУНКЦИИ (опционально) ===
# ElevenLabs API ключ для TTS высокого качества
ELEVEN_API_KEY=ваш_elevenlabs_key
ELEVEN_VOICE_ID=ваш_voice_id
```

#### Как получить ключи:

**1. OpenRouter API ключ:**
- Перейдите на https://openrouter.ai
- Зарегистрируйтесь
- Создайте API ключ в разделе Keys
- Добавьте кредиты для использования моделей

**2. Telegram бот токен:**
- Откройте @BotFather в Telegram
- Отправьте `/newbot`
- Следуйте инструкциям
- Скопируйте полученный токен

**3. ElevenLabs (опционально):**
- Перейдите на https://elevenlabs.io
- Зарегистрируйтесь
- Получите API ключ в настройках профиля

### 4. Запуск бота

```bash
# Из корня проекта
python run.py

# Или из директории backend
cd backend
python main.py
```

После запуска вы увидите:
```
🚀 Запуск LiraAI MultiAssistent v1.0.0
📱 Запуск Telegram polling...
✅ Telegram polling запущен
🎉 Бот полностью инициализирован и готов к работе!
```

### 5. Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по имени
3. Отправьте сообщение "Привет"
4. Бот должен ответить

---

## 🔧 Дополнительные настройки

### Настройка LLM модели

В файле `backend/config.py` можно изменить модель:

```python
LLM_CONFIG = {
    "model": "x-ai/grok-4-fast",  # Основная модель
    "fallback_model": "x-ai/grok-4-fast",  # Резервная
    "temperature": 0.7,
    "max_tokens": 16384,
}
```

Доступные модели на OpenRouter:
- `x-ai/grok-4-fast` - быстрая модель Grok
- `qwen/qwen2.5-vl-32b-instruct` - мультимодальная (для анализа изображений)
- `perplexity/sonar` - для веб-поиска

### Настройка FeedbackBot (работа в группах)

Для автоматической обработки сообщений в группах:

1. Добавьте бота в группу
2. Дайте права администратора (опционально)
3. В `.env` добавьте ID группы:

```env
FEEDBACK_BOT_ENABLED=true
FEEDBACK_BOT_GROUP_IDS=-1001234567890
```

ID группы можно узнать через @userinfobot в Telegram.

### Генерация изображений

По умолчанию используется **Pollinations.ai** (бесплатно, без ключа).

Для использования **Hugging Face**:
```env
HF_API_KEY=ваш_huggingface_key
```

---

## 🐛 Устранение неполадок

### Бот не отвечает

1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что бот запущен (смотрите логи)
3. Проверьте логи в `logs/bot.log`
4. Убедитесь, что у бота есть доступ к интернету

### Ошибка "No module named ..."

```bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Ошибка API ключа

1. Проверьте правильность ключа в `.env`
2. Убедитесь, что на балансе есть кредиты
3. Проверьте статус API на https://status.openrouter.ai

### Проблемы с голосовыми сообщениями

```bash
# Установите дополнительные зависимости
pip install librosa soundfile pydub
```

### Ошибка порта 8000/8001

Если порт занят, измените в `backend/config.py`:

```python
API_CONFIG = {
    "host": "0.0.0.0",
    "port": 8002,  # Измените порт
    "debug": True,
}
```

---

## 📊 Production запуск (PM2)

Для постоянной работы на сервере:

```bash
# Установите PM2
npm install -g pm2

# Настройте путь в ecosystem.config.js
# Замените /root/liraai-multiassistent на ваш путь

# Запустите через PM2
pm2 start ecosystem.config.js

# Сохраните конфигурацию
pm2 save

# Автозапуск при загрузке
pm2 startup
```

---

## 📁 Структура проекта

```
liraai-multiassistent/
├── backend/
│   ├── api/              # Telegram API интеграция
│   ├── core/             # Бизнес-логика
│   ├── llm/              # LLM клиент (OpenRouter)
│   ├── voice/            # STT/TTS
│   ├── vision/           # Анализ/генерация изображений
│   ├── internet/         # Веб-поиск
│   ├── utils/            # Утилиты
│   ├── config.py         # Конфигурация
│   └── main.py           # Точка входа
├── data/                 # Данные
├── logs/                 # Логи
├── temp/                 # Временные файлы
├── requirements.txt      # Зависимости
├── .env.example          # Пример конфигурации
└── run.py                # Скрипт запуска
```

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи: `logs/bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте переменные окружения в `.env`
4. Убедитесь, что API ключи действительны

---

**LiraAI MultiAssistent - Ваш интеллектуальный ассистент в Telegram**
